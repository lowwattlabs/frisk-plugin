{
  "id": "frisk",
  "name": "Frisk",
  "description": "Pre-install security audit and vulnerability scanner for ClawHub skills and code plugins \u2014 scan by slug or local path, 9 threat intel sources, 7 checks including malware scanning, dependency vulnerabilities, and credential leak detection. Wedge paid API available at https://node1.tail507e53.ts.net/frisk for automated scanning at scale.",
  "version": "3.2.1",
  "configSchema": {
    "type": "object",
    "properties": {
      "binaryPath": {
        "type": "string",
        "description": "Absolute path to the frisk binary. Defaults to /usr/local/bin/frisk (not PATH-resolved, to prevent hijacking)."
      },
      "allowExternal": {
        "type": "boolean",
        "description": "Allow network operations: threat intel sync and remote repo cloning. Default: false. Enable only if you trust the agent to contact external services."
      }
    }
  },
  "activation": {
    "onStartup": false
  },
  "contracts": {
    "tools": [
      "frisk"
    ]
  }
}
