//#region src/gateway/server-methods/optional-model-catalog.ts
const OPTIONAL_MODEL_CATALOG_TIMEOUT_MS = 750;
const loggedSlowCatalogKeys = /* @__PURE__ */ new Set();
async function loadOptionalServerMethodModelCatalog(context, surface, options) {
	let timeout;
	const timedOut = Symbol("server-method-model-catalog-timeout");
	const timeoutPromise = new Promise((resolve) => {
		timeout = setTimeout(() => resolve(timedOut), OPTIONAL_MODEL_CATALOG_TIMEOUT_MS);
		timeout.unref?.();
	});
	try {
		const result = await Promise.race([context.loadGatewayModelCatalog().catch(() => void 0), timeoutPromise]);
		if (result === timedOut) {
			const logOnceKey = options?.logOnceKey ?? "session-metadata";
			if (!loggedSlowCatalogKeys.has(logOnceKey)) {
				loggedSlowCatalogKeys.add(logOnceKey);
				context.logGateway.debug(`${surface} continuing without model catalog after ${OPTIONAL_MODEL_CATALOG_TIMEOUT_MS}ms`);
			}
			return;
		}
		return Array.isArray(result) ? result : void 0;
	} finally {
		if (timeout) clearTimeout(timeout);
	}
}
//#endregion
export { loadOptionalServerMethodModelCatalog as t };
