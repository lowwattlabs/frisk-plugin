import { s as normalizeOptionalLowercaseString } from "../../string-coerce-mnp54Vah.js";
import "../../string-coerce-runtime-DzE-BRXv.js";
import { r as resolveStaticCopilotModelOverride } from "../../model-metadata-DVw-uski.js";
//#region extensions/github-copilot/provider-policy-api.ts
const COPILOT_XHIGH_MODEL_IDS = ["gpt-5.4", "gpt-5.3-codex"];
function compatSupportsXHigh(compat) {
	return Array.isArray(compat?.supportedReasoningEfforts) && compat.supportedReasoningEfforts.some((effort) => normalizeOptionalLowercaseString(effort) === "xhigh");
}
function resolveThinkingProfile(context) {
	if (context.provider.trim().toLowerCase() !== "github-copilot") return null;
	const normalizedModelId = normalizeOptionalLowercaseString(context.modelId) ?? "";
	const staticCompat = resolveStaticCopilotModelOverride(normalizedModelId)?.compat;
	return { levels: [
		{ id: "off" },
		{ id: "minimal" },
		{ id: "low" },
		{ id: "medium" },
		{ id: "high" },
		...COPILOT_XHIGH_MODEL_IDS.includes(normalizedModelId) || compatSupportsXHigh(context.compat) || compatSupportsXHigh(staticCompat) ? [{ id: "xhigh" }] : []
	] };
}
//#endregion
export { resolveThinkingProfile };
