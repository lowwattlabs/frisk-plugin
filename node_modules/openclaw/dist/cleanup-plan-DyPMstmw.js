import { _ as resolveOAuthDir, s as resolveConfigPath, y as resolveStateDir } from "./paths-CiHrdfZh.js";
import { i as getRuntimeConfig } from "./io-AmXZn-TT.js";
import "./config-Ba5VqNR9.js";
import { t as buildCleanupPlan } from "./cleanup-utils-CJYxXGIF.js";
//#region src/commands/cleanup-plan.ts
function resolveCleanupPlanFromDisk() {
	const cfg = getRuntimeConfig();
	const stateDir = resolveStateDir();
	const configPath = resolveConfigPath();
	const oauthDir = resolveOAuthDir();
	return {
		cfg,
		stateDir,
		configPath,
		oauthDir,
		...buildCleanupPlan({
			cfg,
			stateDir,
			configPath,
			oauthDir
		})
	};
}
//#endregion
export { resolveCleanupPlanFromDisk as t };
